#include "main.h"
/**
* _abs - Computes the absolute value of an integar
* @n: The integerto be computed
*
* Return: The absolute value of the integar
*/
int _abs(int n)
{
if (n >= 0)
return (n);
else
return (-n);
}
