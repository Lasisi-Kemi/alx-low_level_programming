#include "main.h"

/**
 * _strcat - function that concatenates two strings
 * @dest: first string
 * @src: second string
 * Return: a pointer to the rsulting string
 */

char *_strcat(char *dest, char *src)
{
int x, y;
x = 0;

while (dest[c])
c++;

for (y = 0; src[y] ; y++)

dest[x++] = src[y];
return (dest);
}
